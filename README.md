# 1erPARCIAL - MIÉRCOLES - 09/10/24 -

    Por favor COMPLETAR:
    - NOMBRE:
    - APELLIDO:
    - Cuenta GitHub:

## (1pt.) Ejercicio 1

Dadas las funciones <code>ExVar1()</code>, <code>ExVar2()</code>, y <code>ExVar3()</code>, calcular el resultado de ejecutar cada función considerando el alcanze de sus variables. 

*<u>Nota:</u> Las funciones son ejecutadas en orden*

## (1pt.) Ejercicio 2

Podemos utilizar el siguiente código genera una lista <code>L</code>. Escribir la/s sentencia/s necesarias para generar la misma lista <code>L</code> utilizando el método de *generación de listas por comprensión*. 


## (1pt.) Ejercicio 3

Escribir una función que reciba dos parámetros: 
- *(i) una lista desordenada; y* 
- *(ii) una expresión* (una expresión es parámetro que puede ser evaluado como <code>True</code> o <code>False</code>). Si el valor de la expresión es Verdadera (<code>True</code>), la lista se ordenara en forma descendente, en otro caso de manera ascendente. 

Por defecto, si la función es llamada sin una "expresión" (solo la lista) debe retornar una lista ordenada de forma ascendente.


## (1pt.) Ejercicio 4

Implementar una *función iterativa* que calcule la suma de una lista de números enteros.


## (1pt.) Ejercicio 5

Implementar una *función recursiva* que calcule la suma de una lista de números enteros.


## (2pt.) Ejercicio 6

Definir la clase <code>Circulo</code>. La clase debe contener los siguientes métodos:

- Calcular el perimetro de la círculo.
- Calcular el area del círculo.
- Dado un punto, calcular si este pertenence al círculo.

*<u>Importante:</u>*
- Pueden agregar más atributos y métodos, si lo consideran necesario.
- Pensar en los parametros que los métodos reciben, hay que comprobarlos?

## (2pt.) Ejercicio 7

Sobrecargar los métodos <code>\_\_eq\_\_</code>, <code>\_\_gt\_\_</code>, y <code>\_\_lt\_\_</code>, de la clase <code>Circulo</code>. Es decir, dados dos círculos, comparar si son iguales, comparar si un círculo es más grande o más pequeño que otro, respectivamente.

*<u>Importante:</u>*
- Pensar en los parametros que los métodos reciben, hay que comprobarlos?

## (1pt.) Ejercicio 8

Agregar a la clase <code>Circulo</code> un método que permita calcular lo siguiente:

- Dados dos puntos, calcular si la recta que pasa por ellos es tangente ó secante, al círculo.
- Si la recta no intersecta al círculo, el método retornar un error.

*<u>Importante:</u>*
- Pensar en los parametros que los métodos reciben, hay que comprobarlos?